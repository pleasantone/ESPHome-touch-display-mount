# Gitignore settings for ESPHome
/.esphome/

# Symlink to ../../secrets-esphome.yaml, which uses the same key names as
# /config/esphome/secrets.yaml in the ESPHome Device Builder add-on
# (ESPHome only looks for secrets next to the config)
/secrets.yaml
